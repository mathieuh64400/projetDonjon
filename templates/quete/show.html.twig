{% extends 'base.html.twig' %}

{% block title %}Quete{% endblock %}

{% block body %}
    <h2>Quete</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quete.id }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td><image src="{{ asset('uploads/' ~ quete.filename) }}"></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quete_index') }}">back to list</a>

    {# <a href="{{ path('quete_edit', {'id': quete.id}) }}">edit</a> #}

    {{ include('quete/_delete_form.html.twig') }}
{% endblock %}
