
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          	<a class="nav-link" href="{{path('accueil')}}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link"  href="{{path('sys_connexion')}}">Compte</a>
        </li>
        <li class="nav-item">
        {% if is_granted('ROLE_ADMIN') %}
				<a class="nav-link" href="{{path('admincontrol_index')}}">ADMINISTRATION</a>
				{% elseif is_granted('ROLE_USER') %}

			{% endif %}
         
        </li>
        <li class="nav-item">
        {% if app.user %}
          <a class="nav-link" href="{{ path('app_logout') }}">Deconnexion</a>
          	{% else %}
          	<span class="mb-3 " style="color:white;margin-top:10%;">
						déconnecté!!
					</span>
           {% endif %}
        </li>
        
      </ul>
      {% if app.user %}
      <span style="margin-left:66%; color:white;"> Connecté en tant que {{ app.user.username }} </span>{% endif %}
    </div>
  </div>
</nav>
